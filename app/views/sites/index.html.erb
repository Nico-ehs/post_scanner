<ul>
  <% @sites.each do |site| %>
    <li>
      <%= link_to "#{site.sitename}", site_url(site) %>
      <%= "Posts: #{site.posts.count} Comments: #{site.comments.count}  " %>
    </li>
  <% end %>
</ul>